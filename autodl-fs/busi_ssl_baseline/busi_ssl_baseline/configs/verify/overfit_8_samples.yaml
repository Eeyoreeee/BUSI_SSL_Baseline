base: ../base_sup.yaml

experiment:
  name: overfit_8_samples
  notes: Engineering verification config for supervised overfit-on-8-samples checks.

data:
  train_split: overfit_8_samples.txt
  val_split: overfit_8_samples.txt
  test_split: overfit_8_samples.txt
  num_workers: 0
  persistent_workers: false

train:
  epochs: 300
  batch_size: 2
  log_interval: 1
